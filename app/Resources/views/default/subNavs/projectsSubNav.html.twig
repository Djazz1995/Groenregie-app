  <nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-center subnav-bar">
    <div id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto text-center">
        <li class="nav-item active">
          <a class="nav-link px-5 {{ subActive == 'natureGardens' ? 'groenregie-green' : 'white' }}" href="{{ path('impressions_nature_gardens') }}">Natuurlijke tuinen</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-5 {{ subActive == 'modernGardens' ? 'groenregie-green' : 'white' }}" href="{{ path('impressions_modern_gardens') }}">Moderne tuinen</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-5 {{ subActive == 'patios' ? 'groenregie-green' : 'white' }}" href="{{ path('impressions_patios') }}">Dakterassen</a>
        </li>
      </ul>
    </div>
  </nav>