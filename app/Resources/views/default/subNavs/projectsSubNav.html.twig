  <nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-center subnav-bar">
    <div id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto text-center">
        <li class="nav-item">
          <a class="nav-link px-5 {{ subActive == 'modernGardens' ? 'groenregie-green' : 'white' }}" href="{{ path('impressions_modern_gardens') }}">{{ entry.getModerneTuinenKeuzeMenuTitle() }}</a>
        </li>
        <li class="nav-item active">
          <a class="nav-link px-5 {{ subActive == 'natureGardens' ? 'groenregie-green' : 'white' }}" href="{{ path('impressions_nature_gardens') }}">{{ entry.getNatuurlijkeTuinenKeuzeMenuTitle() }}</a>
        </li>
        <li class="nav-item">
          <a class="nav-link px-5 {{ subActive == 'roofGardens' ? 'groenregie-green' : 'white' }}" href="{{ path('impressions_roof_gardens') }}">{{ entry.getDaktuinenKeuzeMenuTitle() }}</a>
        </li>
      </ul>
    </div>
  </nav>